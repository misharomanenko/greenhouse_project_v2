export declare enum Device {
    DESKTOP = "Desktop device",
    MOBILE = "Mobile device",
    UNKNOWN = "Unknown device"
}
export declare const determineDeviceType: (userAgent: string) => Device;
export declare const SAFARI_CROSS_DEVICE_DUPLICATE_REG_ERROR_MESSAGE = "At least one credential matches an entry of the excludeCredentials list in the platform attached authenticator.";
export declare const CHROME_CROSS_DEVICE_DUPLICATE_REG_ERROR_MESSAGE = "The user attempted to register an authenticator that contains one of the credentials already registered with the relying party.";
