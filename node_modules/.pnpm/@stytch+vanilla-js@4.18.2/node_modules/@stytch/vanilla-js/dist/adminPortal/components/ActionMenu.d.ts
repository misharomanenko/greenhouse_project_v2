import React from 'react';
import { TableActionCoreProps } from '../shared/components/TableActions';
import { Action } from './IconButtonMenu';
export { type Action } from './IconButtonMenu';
export interface ActionMenuProps<T> {
    actions: readonly Readonly<Action<T>>[];
    item: T;
    idPrefix: string;
}
export declare const ActionMenu: <T>({ actions, item, idPrefix }: ActionMenuProps<T>) => React.JSX.Element;
interface UseActionMenuParams<T> {
    actions: readonly Readonly<Action<T>>[];
    idPrefix: string;
    getId: (item: T) => string;
}
export declare const useActionMenu: <T>({ actions, idPrefix, getId }: UseActionMenuParams<T>) => {
    getItemActionProps: (item: T) => TableActionCoreProps;
};
