import { Theme } from '@material-ui/core';
import { StyleConfig } from '@stytch/core/public';
import { PartialDeep } from 'type-fest';
import { AdminPortalStyleConfig } from '../AdminPortalStyleConfig';
export declare const getTheme: (styles?: PartialDeep<AdminPortalStyleConfig>) => Theme;
export declare const useAppTheme: (styleConfig: StyleConfig) => Theme;
