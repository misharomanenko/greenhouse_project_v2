import React from 'react';
import { ModalStateType } from '../components/Modal';
interface InviteModalProps {
    modalState: Omit<ModalStateType, 'open'>;
    inviteName: string;
    setInviteName: (value: string) => void;
    inviteEmail: string;
    setInviteEmail: (value: string) => void;
    inviteRoles: string[];
    setInviteRoles: (value: string[]) => void;
}
export declare const useInviteModal: (revalidateMemberList: () => void) => {
    handleInviteClick: () => void;
    inviteModalProps: {
        modalState: {
            confirm: () => Promise<void>;
            isOpen: boolean;
            close: () => void;
        };
        inviteName: string;
        setInviteName: React.Dispatch<React.SetStateAction<string>>;
        inviteEmail: string;
        setInviteEmail: React.Dispatch<React.SetStateAction<string>>;
        inviteRoles: string[];
        setInviteRoles: React.Dispatch<React.SetStateAction<string[]>>;
    };
};
export declare const InviteModal: ({ modalState, inviteEmail, inviteName, inviteRoles, setInviteEmail, setInviteName, setInviteRoles, }: InviteModalProps) => React.JSX.Element;
export {};
