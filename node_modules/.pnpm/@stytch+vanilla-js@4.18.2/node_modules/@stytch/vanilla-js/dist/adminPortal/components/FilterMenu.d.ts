import React from 'react';
export interface FilterMenuProps<T = string> {
    anchorEl?: HTMLElement;
    items: readonly {
        label: string;
        value: T;
    }[];
    value: Set<T>;
    onChange: (value: Set<T>) => void;
}
export declare const FilterMenu: <T extends string>({ anchorEl, items, onChange, value }: FilterMenuProps<T>) => React.JSX.Element;
