import React from 'react';
import { FormSectionBody } from '../utils/FormSectionBody';
import { Organization } from '@stytch/core/public';
interface OrgDetailsFormState {
    organizationName: string;
    organizationSlug: string;
}
interface OrgSettingsDetailsBodyProps extends FormSectionBody<OrgDetailsFormState> {
    canSetName: boolean | undefined;
    canSetSlug: boolean | undefined;
}
export declare const useMutateOrgSettingsDetails: () => {
    setOrgDetails: ({ organizationName, organizationSlug, }: {
        organizationName?: string | undefined;
        organizationSlug?: string | undefined;
    }) => Promise<void>;
};
export declare const OrgSettingsDetailsBody: ({ canSetName, canSetSlug, remoteState, localState, setLocalState, }: OrgSettingsDetailsBodyProps) => React.JSX.Element;
export declare const OrgSettingsDetailsSection: ({ orgInfo }: {
    orgInfo: Organization;
}) => React.JSX.Element;
export {};
