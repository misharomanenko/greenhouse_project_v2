export interface MutationOptions {
    errorMessage?: string | ((e: unknown) => string);
}
export type MutationFn<T, TReturn> = (properties: T, options?: MutationOptions) => Promise<TReturn>;
export declare const useMutateWithToast: <TParams, TReturn = void>(callback: (params: TParams) => Promise<TReturn>) => {
    mutate: MutationFn<TParams, TReturn>;
};
