import { FC, ReactNode } from 'react';
import { UseBlockNavigation } from '../Router';
type SettingsContextType = {
    editing: boolean;
    setEditing: (editing: boolean) => void;
};
export declare const useSettingsContainer: () => SettingsContextType;
type Props = {
    hasCTA?: boolean;
    onCancel?: () => void;
    onSave?: () => Promise<void>;
    title: string;
    canEdit?: boolean;
    disableSave?: boolean;
    customCTA?: JSX.Element;
    creating?: boolean;
    editing?: boolean;
    setEditing?: (value: boolean) => void;
    modalDescription?: string;
    children: ReactNode;
    useBlockNavigation: UseBlockNavigation;
};
export declare const SettingsContainer: FC<Props>;
export {};
