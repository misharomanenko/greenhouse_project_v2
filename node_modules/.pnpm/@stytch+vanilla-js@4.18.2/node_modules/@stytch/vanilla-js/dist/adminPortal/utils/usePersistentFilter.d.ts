export declare const usePersistentFilter: <T extends string>({ viewId, fieldId, defaultValue, permittedValues, }: {
    viewId: string;
    fieldId: string;
    defaultValue: ReadonlySet<T>;
    permittedValues: ReadonlySet<T>;
}) => readonly [Set<T>, (newValue: ReadonlySet<T>) => void];
