import { PopoverOrigin } from '@material-ui/core';
import React from 'react';
export interface Action<T> {
    key: string;
    label: string;
    icon?: React.ReactNode;
    onClick: (item: T) => void;
    isDangerous?: boolean;
    isVisible?: boolean | ((item: T) => boolean);
}
interface IconButtonMenuProps<T> {
    actions: readonly Readonly<Action<T>>[];
    anchorEl?: HTMLElement;
    closeOnClick?: boolean;
    item: T;
    idPrefix: string;
    icon: React.ReactNode;
    menuAnchorOrigin?: PopoverOrigin;
    menuTransformOrigin?: PopoverOrigin;
    onOpen?: () => void;
    onClose?: () => void;
}
export declare const IconButtonMenu: <T>({ actions, anchorEl: anchorElProp, closeOnClick, icon, item, idPrefix, menuAnchorOrigin, menuTransformOrigin, onClose, onOpen, }: IconButtonMenuProps<T>) => React.JSX.Element;
export {};
