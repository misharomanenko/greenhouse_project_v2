/// <reference types="react" />
export interface StateProps<TState, TRemoteState = TState> {
    localState: TState;
    setLocalState: (state: React.SetStateAction<TState>) => void;
    remoteState: TRemoteState;
}
export declare const useFormState: <TFormState>({ remoteState }: {
    remoteState: TFormState;
}) => {
    localState: TFormState;
    setLocalState: import("react").Dispatch<import("react").SetStateAction<TFormState>>;
    editing: boolean;
    handleSetEditing: (newEditing: boolean) => void;
};
