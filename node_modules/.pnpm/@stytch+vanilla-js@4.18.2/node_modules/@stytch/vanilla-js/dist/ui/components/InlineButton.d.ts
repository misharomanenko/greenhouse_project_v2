import React from 'react';
interface InlineButtonProps {
    onClick: () => void;
    children: React.ReactNode;
}
export declare const InlineButton: ({ onClick, children }: InlineButtonProps) => React.JSX.Element;
export {};
