import React from 'react';
type Props = {
    walletTypeTitle: string;
    icon?: JSX.Element;
    onClick: () => void;
};
export declare const WalletButton: (props: Props) => React.JSX.Element;
export {};
