import React from 'react';
type VariantType = 'warning' | 'success' | 'error';
type AlertBoxProps = {
    variant: VariantType;
    text?: string;
};
export declare const AlertBox: ({ variant, text }: AlertBoxProps) => React.JSX.Element | null;
export {};
