import { BootstrapData } from '@stytch/core';
export declare class CaptchaProvider {
    private bootstrapPromise;
    private state;
    constructor(bootstrapPromise: Promise<BootstrapData>);
    executeRecaptcha: () => Promise<string | undefined>;
}
